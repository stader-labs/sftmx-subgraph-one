{
  "name": "sftmx-subgraph-one",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ stader-labs/sftmx-subgraph-one",
    "create-local": "graph create --node http://localhost:8020/ stader-labs/sftmx-subgraph-one",
    "remove-local": "graph remove --node http://localhost:8020/ stader-labs/sftmx-subgraph-one",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 stader-labs/sftmx-subgraph-one"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
